# 2026-09-12 本地整合：作者 2.98.2 + 必要二改

本地版本：`2.98.2-page0x00.1`。本轮没有 commit、push 或更新 GitHub 二改版；分支 HEAD 仍为 `5d1b428c284d024d558f273973cbb406a3b86456`。修改和新增文件均保留在工作区，待实机测试。

## 作者版本核对

已执行 `git fetch origin main`。当前 `origin` 和 `mine` 都指向 `page0x00/ST-Preset-Workshop`；该分叉的 `origin/main` 仍为 `a52e18bd6fb1f10e2d700f110c0a8ffeefc1e46e`（2.97.36）。因此又从作者仓库单独 fetch `main`，没有更改现有 remote 配置。

本次整合的作者最新提交：

- 仓库：[yui-ovo/ST-Preset-Workshop](https://github.com/yui-ovo/ST-Preset-Workshop)
- 提交：[6db8d7ef376716def096ffdfdfa812baf5ec3ca5](https://github.com/yui-ovo/ST-Preset-Workshop/commit/6db8d7ef376716def096ffdfdfa812baf5ec3ca5)
- 版本：`2.98.2`
- 作者：`yui-ovo`
- 时间：2026-09-12 05:26:16 +08:00
- 提交说明：`docs: trim patch release notes`

## 本次更新

- 接入作者世界书快照及组合方案：预设默认、角色/聊天绑定、分组方案、草稿、冲突处理与恢复流程。
- 采用作者独立轻量快照编辑器，保留原生相机入口；分组展开和正文预览按需加载，保存/取消保持主界面正文草稿独立。
- 接入作者分支差异重基修复：继承字段随主预设更新，显式分支差异和正在编辑的内容继续保留。
- 接入作者世界书拖入预设时读取未保存目标草稿、排队后重新定位当前目标及刷新时保留脏草稿的修复。
- 保留作者桌面四角居中缩放、尺寸记忆、双击恢复默认宽度并保留高度，以及移动端视口、快照名称复制等更新。

## 必要二改与冲突处理

以 2.97.36 为共同基线，对作者更新与现有工作区做三方合并；5 个文件发生冲突，均已处理。作者变更的 48 个文件中，42 个保持逐字节一致；其余为 README、启动入口、业务入口、清单、package 和版本校验文件。

删除作者新编辑器已替代的旧录制模式 DOM 补丁、主窗口录制监听器与对应二改 CSS；旧录制回归改为新版轻量编辑器的保存/取消与草稿隔离回归。版本一致性校验采用作者新版的动态校验，另保留 package 版本一致检查。

继续保留必要的中控、四种美化、条幅、草稿缓存与安卓性能差异：

- 条幅默认按完整预设名称测宽，超过视口时限制在屏幕内；手动宽度优先保留，溢出直接裁切，不加省略号。
- 中控数值保存即时同步，连续编辑与拖动按帧合并，切皮肤复用颜色测量结果。
- 条目正文颜色修复覆盖原生、内联和独立放大编辑器，只改文字色；aqua/violet 冻结材质与视觉基线哈希未修改。
- 保留当前酒馆预设的权威选择、未保存草稿、滚动续载、分组空闲停止扫描和运行时回收。
- 新桌面缩放的监听限定到工坊结构；聊天更新和缩放手柄自身变更不触发扫描，连续有效变化合并一帧，纯触屏设备不监听面板子树。卸载会释放监听和待执行帧。
- 桌面手动四角尺寸优先于中控的默认宽度，双击时依据当前中控默认宽度恢复。现有中控标题区布局作为自定义差异保留，材质不重设计。

## 验证

通过：

- `npm run check`，包含作者 precheck、check、世界书快照 postcheck。
- `npm run check:ui`，覆盖条幅默认宽度/手动设置/横竖屏边界、中控即时保存、连续调节、切皮肤缓存、草稿重启恢复、冻结材质、聊天保护、拖动与回收。
- `npm run test:worldbook-unsaved-target`。
- 新旧集成回归：轻量快照两轮保存/取消不改正文草稿与重启缓存；桌面缩放忽略 240 次无关变化，有效变化合并一帧。
- JavaScript 语法、冲突标记检查、`git diff --check`。

环境为 Termux + Node/VM，没有可用的浏览器/Playwright，未运行作者需要 MS Edge 的浏览器布局套件，也未完成安卓实机视觉和帧率验收。自动检查不能代替实机测试。

## 本地试用

需让扩展实际加载这个工作区的文件，再刷新酒馆。可核对扩展版本是否为 `2.98.2-page0x00.1`。只拉取 GitHub 二改版不会获得本轮修改；本轮明确不发布。

未改动用户文件 `preset-v2.48-p39.json`，没有提交或修改上传人信息。以后同步需由用户明确要求。
