# 2.98.12-page0x00.1

同步作者 2.98.12（`bc7e746`），发布目标为 `page0x00/ST-Preset-Workshop` 的 `二改版` 分支。

- 收藏多选拖入文件夹、拖回根目录及指定条目前后，统一批量保存，保留可见顺序与正确归属。
- 应用快照关闭未记录条目，避免同名条目误匹配；失效快照在写入前拒绝。
- 保留中控、四种美化、条幅、草稿、悬浮吸附和安卓性能优化，Aqua/Violet 冻结材质不变。
- 修复 3 个旧快照测试，验证新版原生入口及独立编辑器，并纳入默认检查；没有恢复旧录制 UI 或增加运行时补丁。

默认功能检查、二改 UI 检查及新版快照流程全部通过；上一轮同一份运行代码已通过条幅、悬浮吸附与手机拖动浏览器回归。

[整合、验证与边界说明](upstream-integration-20260915.md)。
