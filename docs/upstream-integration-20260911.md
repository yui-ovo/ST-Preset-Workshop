# 作者 2.97.36 整合记录

本次在现有 `二改版` 工作区整合最新 `origin/main`，保留待实机验收的未提交合并。没有 commit、push，也没有同步其他安装目录或更改远端配置。

## 作者版本核对

- 已执行 `git fetch origin main`。
- `origin` 为 `https://github.com/page0x00/ST-Preset-Workshop.git`；其 `main` 最新提交为 `a52e18bd6fb1f10e2d700f110c0a8ffeefc1e46e`。
- 同时通过 `git ls-remote https://github.com/yui-ovo/ST-Preset-Workshop.git refs/heads/main` 核对作者仓库，得到同一个提交。
- 作者：`yui-ovo`；提交时间：2026-09-09 18:10:36 +0800。
- 最新提交：`fix(tauri): isolate native preset event boundary`；扩展版本：`2.97.36`。
- 二改版原 HEAD：`82edd14`；上次作者基线：`257f386`（2.97.29）。本次合入其后的全部 9 个提交的最终结果。

## 更新与冲突处理

| 区域 | 最终实现 |
| --- | --- |
| 快照进入与退出 | 使用作者完整冻结基线及来源上下文；保存／取消后恢复进入前状态，原生相机不继承隐藏工坊的旧脏标记 |
| 录制中的条目与柏宝箱分组 | 使用作者隔离画布；录制期间不提前写入命名预设或原生分组，退出后仍恢复正常同步 |
| 主预设来源页 | 使用作者 body 冒泡阶段的事件边界，仅匹配五个明确工坊根节点，保留 Tauri／酒馆其他按钮事件 |
| 批量预设搜索 | 使用作者 NFKC 归一化、明确隐藏样式、中文输入完成／搜索清除事件及无匹配提示 |
| 原生入口发现 | 合并作者事件边界与现有 `stopDiscovery()`：先同步绑定边界及入口，就绪后停止发现；尚未就绪时保留局部观察及按使用发现 |
| 卸载 | 原作者清理函数负责删除 body 监听，现有二改生命周期负责调用该函数；重复加载不叠加监听 |
| 版本及回归 | 清单与启动器升级 2.97.36；作者 `check`、`precheck` 命令与 `origin/main` 完全一致，另保留二改 UI 回归 |

实际文本冲突为 `README.md`、`package.json`、`dist/workshop-v3.02.js`，均已解决并标记完成，没有遗留冲突条目。README 保留作者所有最新发布说明；package 保留作者全部测试并追加现有 UI 检查。

作者期间尝试的 `d125995`、`95b6d94` 已被其后两个 revert 撤回，本次遵循作者最终状态，不重新引入旧抽屉保留方案。原来退出时从当前草稿重新合并开关、录制中写分组只抑制提示的旧路径已随作者更新替换。

## 二改差异审计

快照录制／退出、开关隔离、搜索过滤及精确事件边界的核心代码已经逐段与 `origin/main` 比较一致，没有另设二改业务实现。

现有草稿跨重启缓存、酒馆当前选择优先、分支只读识别解决的是启动与重开状态，作者这轮的录制事务没有替代这些行为，继续保留。自定义布局、中控、悬浮入口、拖动按帧合并、延迟续载、局部观察、实例清理和聊天更新刷新保护也仍有独立用途。本次未增加新的产品自定义或重写上述模块。

`dist/worldbook-stitch-test3.js`、`dist/preset-content-editor.js`、`manifest.json` 与作者最新版逐字节一致；`bridge/`、`legacy/` 与作者无差异。作者本轮新增／更新文件全部保留。

冻结约束检查：

- `dist/workshop-theme-system.js` 与本次开始时 HEAD 逐字节一致，SHA256：`a8d17011521f899b95c155bb17a0734d3ae6457dc752a8d6b2d39c098fbd5ba3`。
- `scripts/fixtures/workshop-frozen-visuals.json` 原样保留，SHA256：`a82158a143b654eb2d3c28cf997c3a9659bac7c5957a5506d75d21226cdb95cb`。
- Aqua、Violet、已批准 Aqua 主界面例外、所有条幅材质均未改动；本轮新增 CSS 仅为作者批量管理弹窗的筛选与空结果样式。
- 原有未跟踪文件 `preset-v2.48-p39.json` 未改动、未暂存。

## 检查结果

- `npm run check`：通过，包含作者 `precheck`，共 127 份作者当前启用回归及入口检查。
- `npm run check:ui`：通过，包含 10 份二改／融合回归及 6 个自定义模块语法检查。
- 新增 `scripts/test-upstream-v29736-integration.mjs`：执行搜索过滤、事件边界、三次重复安装与清理、跨异步打开保留原生来源上下文，以及实际预设 store＋缓存＋作者录制事务。覆盖连续两次录制、干净／未保存／原生来源、录制中写入隔离、正常退出后的缓存重启恢复与开关同步。
- 其余 8 份历史测试也已执行：3 份通过，5 份失败；5 份均在从本次最新 `origin/main` 导出的独立干净副本中复现。
- 共 145 份测试：140 份通过，5 份为上述双方共有的历史断言失败。
- 211 份 JS／MJS 语法检查通过；`git diff HEAD --check` 通过；`git ls-files -u` 为空。

| 未纳入作者当前检查命令的历史失败 | 本次纯 origin/main 同样失败的原因 |
| --- | --- |
| `test-test42-one-way-worldbook-drop.mjs` | 旧 transfer 文本边界断言与现有落点清理代码不符 |
| `test-v268-gecko-render.mjs` | 要求已撤回的 Gecko iframe 渲染实验 |
| `test-v269-normal-runtime.mjs` | 要求启动器仍指向 v2.69 |
| `test-v270-split-title-widths.mjs` | 要求启动器仍指向 v2.70 |
| `test-v296-group-select-tauri.mjs` | 要求扩展版本仍为 2.96.0 |

没有修改这些旧测试来掩盖失败。运行回归使用 Node／VM 模拟，不等于真实浏览器或 WebView 验收，实机帧率和触摸观感仍待确认。

## 实机验收重点

1. 从酒馆主预设小相机进入，连续制作两次快照，分别保存／取消；确认条目与柏宝箱分组恢复进入前状态，原主预设页保留。再从工坊中带未保存正文修改录制，确认退出后草稿与保存高亮保留。
2. 批量预设搜索输入中文、全角英文、无匹配内容，再清空；检查列表、空结果提示与筛选后的全选。
3. Tauri／SillyTavern 扩展开关、管理、删除以及普通聊天按钮仍能正常点击；停止／启动扩展后无重复入口或重复响应。
4. 手机／平板检查悬浮入口、条幅、中控、双面板拖动及保存／取消，确认尺寸、字号、冻结主题与流畅度保持原有表现；长列表仍能滚动到最后一个延迟加载条目。

工作区停留在“冲突已解决、尚未提交”的合并状态。等待用户实机确认后再处理提交及 `mine/二改版` 同步。
