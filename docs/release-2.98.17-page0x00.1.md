# 2.98.17-page0x00.1

同步作者 2.98.17（`da6ecb1`），发布目标为 `page0x00/ST-Preset-Workshop` 的 `二改版` 分支。

- 桌面放大正文按视口居中；缝合界面两侧可同时编辑，保存、撤销和取消相互独立，手机保留单编辑器。
- 应用快照后可覆盖保存当前调整或另存新快照，预设默认仍有保护。
- 手动全局应用后的开关调整会保留，显示“已调整”并可恢复；角色／聊天绑定仍按作者规则优先。
- 快照读取当前酒馆尚未保存的实时开关，其他预设和隐藏工坊草稿继续隔离。
- 新桌面编辑器保留二改版原有文字色；聊天更新及正文输入不再引发主界面布局读取，关闭和卸载释放监听与浮层。

作者快照模块完整保留原实现。中控、条幅、四种美化和安卓性能优化保留，Aqua/Violet 冻结材质不变。

[详细整合与验证记录](upstream-integration-20260919.md)。
