# 2.98.2-page0x00.1：条幅默认宽度与正文文字修复

> 本文记录上一轮修复；本轮条幅规则与版本以[条幅名称与宽度规则](banner-contract-20260912.md)为准。

- 条幅默认按当前预设完整名称的实际字体宽度，加上按钮、分支栏和手柄留位计算展开宽度。支持手机、平板横竖屏与电脑；过长名称最多占满视口。
- 手动调窄时直接裁切溢出文字，不用三个点或省略号代替。真实预设名称、选项和酒馆当前选择不作截短或改写。
- 中控手动设置的宽度优先保留；切换预设、保存、重新加载均不能用自动宽度覆盖它。未自定义时，中控数值与测得的默认宽度同步。
- 正文文字色覆盖原生编辑、内联编辑、独立放大编辑以及加载世界书后共用的放大编辑器。四种皮肤和酒馆跟随模式均采用当前文字色；只改文字颜色，保留背景、材质和搜索输入层的透明行为。
- 保留按帧拖动、保存即时同步、单项控件更新、连续编辑合并校正与切皮肤缓存。名称宽度测量在拖动期间延后，结束后合并执行。

前两轮修复没有推送，远端当时仍为 `5d1b428`，所以仅拉取远端不会获得那些修改。本次将它们与作者 2.98.2 更新、默认宽度修复一起保留在本地，并将扩展入口、清单、package 和运行模块缓存参数统一更新为 `2.98.2-page0x00.1`。

验证：`npm run check`、`npm run check:ui`、冻结材质哈希和 `git diff --check` 均通过。回归覆盖默认名称宽度、视口边界、手动设置保存/重载、所有条幅尺寸控件、连续调节与切皮肤、任务回收及聊天刷新保护。测试环境为 Node/VM，尚未完成安卓真实浏览器的视觉与帧率验收。

按 2026-09-12 最新要求，本轮不 commit、不 push，也不同步 GitHub 二改版。远端仍是旧版，本地试用需加载工作区文件；以后同步须由用户明确要求。
